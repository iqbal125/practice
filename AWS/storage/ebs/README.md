# EBS (Elastic Block Store)

Block-level storage volumes for use with EC2 instances. Provides persistent storage that persists independently from instance lifetime. Supports different volume types optimized for performance or cost.
